using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickupPosition : MonoBehaviour {

	//// Use this for initialization
	//void Start () {
		
	//}
	
	//// Update is called once per frame
	//void Update () {
 //       // setting target position for pickup to sit a set distance on all surfaces
 //       RaycastHit _hit;
 //       if (Physics.Raycast(transform.position, Vector3.down, out _hit, 100f))
 //       {
 //           // calculate new position from alternate surface 
 //           this.transform.position = new Vector3(0f, -_hit.point.y, 0f);
 //       }
 //       else
 //       {
 //           // assume the player has hit the ground level
 //           this.transform.position = new Vector3(0f, 0f, 0f);
 //       }

 //   }
}
